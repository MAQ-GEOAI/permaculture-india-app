# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Essential build tools (fixes setuptools error)
setuptools==69.0.0
wheel==0.42.0
pip==24.0

# Data processing
numpy==1.24.3
scipy==1.11.4
requests==2.31.0
# Note: scipy required for professional-quality contour interpolation (griddata, gaussian_filter)

# Geospatial (rasterio needs GDAL, but we'll handle it differently)
rasterio==1.3.9; sys_platform != "win32"
rasterio==1.3.9; sys_platform == "win32"

# Optional - install if available
pysolar==0.10
python-dotenv==1.0.0

# AI (optional)
openai==1.3.0

# Note: whitebox is optional - code will work without it
# GDAL/rasterio may need system packages on some platforms
